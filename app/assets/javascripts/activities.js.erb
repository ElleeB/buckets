// Activity Prototype //
function Activity(title, categoryID, description, dueDate) {
	this.title = title,
	this.categoryID = categoryID,
	this.description = description,
  this.dueDate = dueDate,
	
	this.dueDate = function() {
		// return this.title.toUpperCase() + " | " + this.user
	}
}
